name: macro_app
description: "A native Flutter client for Macro Unified Workspace."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  app_links: ^7.2.1
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^9.2.2
  google_fonts: ^6.2.1
  http: ^1.2.0
  intl: ^0.19.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0
  uuid: ^4.5.1
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
